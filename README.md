# Node Search Algorithm

Application that creates a graph from a JSON file. It is created to study different pathfinder algorithms like Dikjstra.

The repository is divided in different folders:
- **Classes:** the network, nodes and legs are defined in here.
- **Network:** here are some examples of JSON files for creating the network
